version: '3.8'
services:
  dashboard:
    build: .
    ports:
      - "8080:8080"
    environment:
      - REDIS_ADDR=redis:6379
      - AWS_REGION=us-east-1
    depends_on:
      - redis
  redis:
    image: "redis:alpine"
    ports:
      - "6379:6379"