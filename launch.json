{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Attach to Remote (Atlas)",
            "type": "go",
            "request": "attach",
            "mode": "remote",
            "remotePath": "${workspaceFolder}",
            "port": 2345,
            "host": "127.0.0.1",
            "apiVersion": 2,
            "showLog": true
        }
    ]
}